// const { createCart, getCart, getCarts, updateCart, deleteCart } = require('../controllers/cart')
// const { authUserAndAdmin, auth, authUserAndAuthorize } = require('../middlewares/auth')

// const router = require('express').Router()

// router.post('/', auth, createCart)
// router.get('/', auth, getCarts)
// router.get('find/:id', auth, getCart)
// router.put('/:id', authUserAndAuthorize, updateCart)
// router.delete('/:id', authUserAndAuthorize, deleteCart)

// module.exports = router